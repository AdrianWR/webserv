Parse config ok
	validacao (?)

Setup dos sockets (a partir da config) ok

Ouvir sockets
	Multiplex ok
	Pedacos 
	Chunks 
	Produz um http request object ok

Handler
	GET
		devolve redir ok
		1.devolve arquivo ok
		2.devolve dir (autoindex ou index ou erro) ok
		3.devolve cgi
		devolve pag de erro se falhar 1 2 3 ok
		Strings precisam gerar http response object ok

	POST ok
		check size ok
		fetch_cgi

	DELETE ok

Responder socket
	Devolver http response serializado ok
	Fechar conexao ok

============================================================================
conectar request no handler ok

criar http response ok
conectar o handler no http response ok

handler do post ok
handler do delete ok
cgi

criar sites de teste ok
criar testadores para principais casos

=========================================================================
Estrutura do site
www
	site1
		aaa (getnok)
		bbb (getok) [arquivo existe / nao existe / diretorio sem autoindex]
		ccc (redirection)
		ddd (diretorio) [com index]
		eee (diretorioa) [com autoindex]
		fff (cgi)
	site2
		ggg (uploadok) [arquivo / arquivo gde]
		hhh (noupload) [metodo nao permitido]
		iii (diretorio)
		jjj (cgi)
	site3
		kkk (deleteok) [delete permitido]
		lll (deletenok) [metodo nao permitido
		mmm (sempermissao) [delete sem permissao]

